# TODO — sc_analysis

## NEXT
Review the diffs on `R/summary_helpers.R`, `R/visualization_helpers.R`, and `scripts/01_generate_outlier_summaries.R` + `scripts/06_visualize_sample_dropouts.R`, then commit them with the new sample manifests (`samples/ALLSAMPLES_27686.csv`, `SLX-27548_single.csv`, `SLX-27634_single.csv`). gitignore the caches (`output/R_libs_4.6/`, `output/fontconfig-cache/`, `scAbsolute_obj/`).

## Backlog
- [ ] Decide whether `Rplots.pdf` and `environment.yml` changes are intentional or incidental.
- [ ] Commit `AGENTS.md` if it should be tracked.

## Blockers
- none — this looks like finished QC work that's only at risk from context loss (most stale repo, ~2 months).
